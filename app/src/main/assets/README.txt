Place an English toxicity TensorFlow Lite model here named:

    toxicity.tflite

The ToxicityClassifier will memory-map and use it automatically.
Without this file, the classifier falls back to a transparent
heuristic scorer so the full pipeline still runs.
